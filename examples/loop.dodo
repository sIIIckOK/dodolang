0 for dup 20 < do
    dup 5 swap divmod 
    swap drop 0 = if
        7155 print
    end

    dup 10 swap divmod 
    swap drop 0 = if
        8055 print
    end

    dup 15 swap divmod 
    swap drop 0 = if
        71558055 print
    end

    1 +
end
