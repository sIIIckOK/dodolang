macro exit
    60 syscall1 // syscall exit
end

var x int end
x 0 !

for true do
    x @ 10 > if // if x > 10 exit, there is `break` for loop yet
        0 exit
    end
    x x @ 1 + ! // increment value in x
    x @ print   // print x
end

